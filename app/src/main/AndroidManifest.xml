<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.csulb.android.bluetoothmessenger">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Home screen of the app -->
        <activity
            android:name=".ChatActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Screen to choose either server or client -->
        <activity
            android:name=".FrontActivity"
            android:parentActivityName=".MainActivity" />
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".MainActivity" />

        <!-- Screen to show available devices to choose from -->
        <activity
            android:name=".ClientActivity"
            android:label="@string/title_activity_client"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />

        </activity>

        <!-- Screen to send messages from client side -->
        <activity
            android:name=".ConnectedActivity"
            android:label="@string/title_activity_connected"
            android:parentActivityName=".MainActivity"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

        <!-- Screen to send messages from server side -->
        <activity
            android:name=".ServerActivity"
            android:label="@string/title_activity_server"
            android:parentActivityName=".MainActivity"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

        <!-- Screen to view and edit the user profile -->
        <activity
            android:name=".ProfileActivity"
            android:label="@string/title_profile"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

        <!-- Chat activity -->

        <activity android:name=".MainActivity" />

        <activity
            android:name=".DeviceListActivity"
            android:label="Select a device to connect"
            android:theme="@android:style/Theme.Holo.Dialog" />
    </application>

</manifest>